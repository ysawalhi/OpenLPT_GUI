✓ Successfully imported BudgetConfig, BudgetStatus, run_global_search

--- Test 1: Dataclass instantiation ---
✓ BudgetConfig created: max_total_evals=50, enable_probing=False
✓ BudgetStatus created: BudgetStatus(probing_evals_used=0, probing_wall_seconds=0.0, probing_stopped_by='', total_evals_used=0, total_wall_seconds=0.0, total_stopped_by='', runs_completed=0, cumulative_by_run=[])
✓ is_reduced_probing() = True (expected: True, probing disabled)
✓ to_dict() returns serializable dict with keys: ['probing_evals_used', 'probing_wall_seconds', 'probing_stopped_by', 'total_evals_used', 'total_wall_seconds', 'total_stopped_by', 'runs_completed', 'cumulative_by_run']

--- Test 2: Reduced probing detection ---
✓ With max_probing_evals=50: is_reduced_probing() = True (expected: True)
✓ With max_probing_evals=500: is_reduced_probing() = False (expected: False)

--- Test 3: Function signature (accepts budget_config parameter) ---
✓ run_global_search has 'budget_config' parameter
  Default value: None (expected: None)

--- Test 4: GlobalSearchResult structure ---
✓ GlobalSearchResult has 'budget_status' field

============================================================
✓ All smoke tests passed!
============================================================

Summary:
  1. BudgetConfig and BudgetStatus dataclasses instantiate correctly
  2. is_reduced_probing() and to_dict() methods work
  3. run_global_search accepts budget_config parameter
  4. GlobalSearchResult includes budget_status field

Task 7 guardrails implementation is syntactically correct and structurally sound.

============================================================
STRICT-BUDGET SMOKE TEST
============================================================

Budget Configuration:
  max_total_evals: 12
  max_probing_evals: 6
  max_per_run_evals: 6
  enable_probing: True
  is_reduced_probing(): True

Expected budget_status behavior:
  ✓ probing_evals_used should be <= 6 (6)
  ✓ total_evals_used should be <= 12 (12)
  ✓ runs_completed reflects how many CMA-ES runs executed
  ✓ cumulative_by_run tracks per-run evaluations

✓ Budget configuration is valid and tightly constrained

Note: Full functional test requires real camera/observation data.
Budget guardrails are in place and will enforce:
  1. Early exit when total_evals >= 12
  2. Probing stops at 6 evals
  3. Each CMA-ES run limited to 6 evals

============================================================
✓ STRICT-BUDGET SMOKE TEST PASSED
============================================================

Budget enforcement structure is correct and ready for integration.
