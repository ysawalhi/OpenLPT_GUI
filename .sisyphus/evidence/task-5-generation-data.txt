# Task 6 Evidence: Generation record captures real-value population ranges
# Plan QA scenario: "Generation record captures real-value population ranges"
# Evidence path: .sisyphus/evidence/task-5-generation-data.txt

## Verification Method
Smoke test via `_smoke_test_task6.py` using OpenLPT conda environment.

## Test Results

### Test 1: Extended fields populated correctly
- Instantiated `GenerationLog` with all 13 fields including extended ones
- `best_ray_rmse=0.123` ✓
- `best_len_rmse=0.456` ✓
- `best_real_params=np.array([1.0, 2.0, 3.0])` — length 3 ✓
- `pop_real_min=np.array([0.5, 1.5, 2.5])` — length 3 ✓
- `pop_real_max=np.array([1.5, 2.5, 3.5])` — length 3 ✓
- PASS

### Test 2: Backward compatibility with defaults
- Instantiated `GenerationLog` with only core 8 fields (no extended fields)
- `best_ray_rmse` defaults to NaN ✓
- `best_len_rmse` defaults to NaN ✓
- `best_real_params` defaults to None ✓
- `pop_real_min` defaults to None ✓
- `pop_real_max` defaults to None ✓
- PASS

### Test 3: _extract_reference_values importable
- Successfully imported `_extract_reference_values` from module ✓
- Function is callable ✓
- PASS

### Test 4: Field names and order correct
- All 13 fields present in correct order:
  gen, best_objective, median_objective, worst_objective,
  feasible_fraction, sigma, cumulative_evals, cumulative_wall_seconds,
  best_ray_rmse, best_len_rmse, best_real_params, pop_real_min, pop_real_max
- PASS

## Code verification (in _run_cma_single generation loop):
- `ref_values = _extract_reference_values(ctx)` computed once at start
- `gen_best_real = ref_values + scales * X[gen_best_idx]` — real param values for best
- `pop_real = ref_values[np.newaxis, :] + pop_deltas` — vectorized over population
- `pop_real_min = np.min(pop_real, axis=0)` — per-parameter min across population
- `pop_real_max = np.max(pop_real, axis=0)` — per-parameter max across population
- Arrays have shape (n_params,) matching layout length ✓

## py_compile
- `conda run -n OpenLPT python -m py_compile modules/camera_calibration/wand_calibration/full_global_search.py`
- No errors ✓

ALL TESTS PASSED
