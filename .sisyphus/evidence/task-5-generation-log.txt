# Task 6 Evidence: Console logs remain concise
# Plan QA scenario: "Console logs remain concise"
# Evidence path: .sisyphus/evidence/task-5-generation-log.txt

## Verification Method
Code inspection of the console log format in `_run_cma_single()` at line ~3080.

## Console log format (unchanged):
```python
logger.info(
    '[Run %d][Gen %d] obj=%.4f ray=%.4f len=%.4f '
    'med=%.4f worst=%.4f sigma=%.4f evals=%d feas=%.0f%%',
    run_id + 1,
    gen_idx + 1,
    gen_best_obj,
    gen_best_ray_rmse,
    gen_best_len_rmse,
    median_obj,
    worst_obj,
    float(es.sigma),
    total_evals,
    feasible_frac * 100,
)
```

## Analysis:
- The console log prints ONE LINE per generation with summary metrics only
- `gen_best_ray_rmse` and `gen_best_len_rmse` are reused from the extended computation
  (not duplicated — same variable feeds both the console log and the GenerationLog record)
- NO per-parameter arrays are printed to console
- NO `best_real_params`, `pop_real_min`, or `pop_real_max` appear in console output
- Format is: `[Run N][Gen M] obj=X.XXXX ray=X.XXXX len=X.XXXX med=X.XXXX worst=X.XXXX sigma=X.XXXX evals=N feas=NN%`

## Conclusion:
Console log remains concise — one summary line per generation, no per-parameter dumps.
Detailed per-parameter data is stored ONLY in the structured `GenerationLog` dataclass records
for later CSV output (Task 7).

PASS
